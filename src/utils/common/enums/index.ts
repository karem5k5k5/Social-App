export enum SYS_ROLE {
    user,
    moderator,
    admin
}

export enum GENDER {
    male,
    female
}

export enum USER_AGENT {
    local,
    google
}

export enum REACTION {
    like,
    love,
    care,
    angry,
    sad,
    wow
}