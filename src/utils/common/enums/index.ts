export enum SYS_ROLE {
    user = "user",
    moderator = "moderator",
    admin = "admin"
}

export enum GENDER {
    male = "male",
    female = "female"
}

export enum USER_AGENT {
    local = "local",
    google = "google"
}